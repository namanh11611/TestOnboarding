package com.coccoc.helloworld2;

import com.google.firebase.messaging.FirebaseMessagingService;

public class CCFirebaseService extends FirebaseMessagingService {
    @Override
    public void onNewToken(String s) {
        super.onNewToken(s);
    }
}
